apiVersion: v1
kind: Service
metadata:
  name: {{ include "muffin-wallet.name" . }}
  labels:
    {{- include "muffin-wallet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "muffin-wallet.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}